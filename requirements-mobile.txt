# =============================================================================
# StreamCatch - Mobile Build Dependencies
# =============================================================================
# Used by: flet build apk / flet build ipa
# 
# IMPORTANT: Only include packages that have mobile-compatible wheels!
# Packages like pyinstaller, pytest, black, etc. will FAIL on mobile builds.
# =============================================================================

# Core Downloading
yt-dlp>=2023.11.16              # YouTube/video downloader library

# UI Framework
flet>=0.28.3                    # Modern UI framework

# Image & Media
Pillow>=10.0.0                  # Image processing for thumbnails

# Networking
requests>=2.31.0                # HTTP library
urllib3>=2.0.0                  # HTTP client
certifi>=2023.0.0               # SSL certificates

# Web Scraping
beautifulsoup4>=4.12.0          # HTML parsing

# =============================================================================
# EXCLUDED from mobile builds (no mobile wheels available):
# - pyinstaller (desktop-only build tool)
# - pytest, pytest-cov (testing tools)
# - black, mypy, pylint (dev tools)
# - PyDrive2 (requires OAuth flow not suitable for mobile)
# - pypresence (Discord - desktop only)
# =============================================================================
